<script setup>
import { RouterLink } from 'vue-router'
import { state } from '../stores/lexigo'
const actions = [{ icon: '＋', title: 'Create a package', copy: 'Add a new vocabulary unit', to: '/packages/new', featured: true }, { icon: '📚', title: 'Vocabulary library', copy: `${state.packages.length} saved packages`, to: '/library' }, { icon: '👥', title: 'My groups', copy: `${state.groups.length} learning groups`, to: '/groups' }, { icon: '📈', title: "Students' progress", copy: 'See growth at a glance', to: '/progress' }, { icon: '⚙️', title: 'Settings', copy: 'Back up your learning space', to: '/settings' }]
</script>
<template><section class="mx-auto max-w-6xl px-5 py-16"><p class="text-[11px] font-bold tracking-[.15em] text-lexi">{{ state.mode === 'kids' ? 'HELLO, EXPLORER' : 'TEACHING DASHBOARD' }}</p><h1 class="mt-2 font-display text-5xl font-bold tracking-tight sm:text-6xl">{{ state.mode === 'kids' ? 'Ready for a word adventure?' : 'Make every lesson count.' }}</h1><p class="mt-4 max-w-xl leading-7 text-slate-500">Create a word package, practise together, and follow progress in one calm place.</p><div class="mt-10 grid gap-4 md:grid-cols-3"><RouterLink v-for="item in actions" :key="item.title" :to="item.to" class="min-h-40 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg" :class="item.featured ? 'bg-gradient-to-br from-lexi to-violet-500 text-white md:col-span-2' : ''"><span class="grid h-12 w-12 place-items-center rounded-xl text-2xl" :class="item.featured ? 'bg-white/15' : 'bg-violet-50'">{{ item.icon }}</span><h2 class="mt-5 font-display text-2xl font-bold">{{ item.title }}</h2><p class="mt-1 text-sm" :class="item.featured ? 'text-violet-100' : 'text-slate-500'">{{ item.copy }}</p></RouterLink></div></section></template>
