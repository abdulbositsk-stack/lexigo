# LexiGo

LexiGo is a Vue 3 vocabulary-learning application built with Vite, Vue Router, and Tailwind CSS.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The original standalone HTML is retained in `legacy/` as a reference during the Vue migration.
